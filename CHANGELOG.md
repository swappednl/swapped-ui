# Changelog

## 0.1.0 — 2026-09-04

- Eerste extractie uit swapped-projects (`resources/css/app.css` op `e9aa615`+): tokens, `@layer base`, alle recepten (kern, keuzelijst, rail, startpagina, mail/agenda/chat, notities, mobiel, handleiding) op `primary-*`-tokens; iconenset (136 paden) als `Swapped\Ui\Icons` en `<x-swapped::icon>`; Alpine-modules `dropdown.js` en `aanwezigheid.js`.
